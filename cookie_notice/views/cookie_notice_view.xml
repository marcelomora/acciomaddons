<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cookie_notice.list">
      <field name="name">cookie_notice list</field>
      <field name="model">cookie_notice.cookie_notice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cookie_notice.action_window">
      <field name="name">cookie_notice window</field>
      <field name="res_model">cookie_notice.cookie_notice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cookie_notice.action_server">
      <field name="name">cookie_notice server</field>
      <field name="model_id" ref="model_cookie_notice_cookie_notice"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cookie_notice" id="cookie_notice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cookie_notice.menu_1" parent="cookie_notice.menu_root"/>
    <menuitem name="Menu 2" id="cookie_notice.menu_2" parent="cookie_notice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cookie_notice.menu_1_list" parent="cookie_notice.menu_1"
              action="cookie_notice.action_window"/>
    <menuitem name="Server to list" id="cookie_notice" parent="cookie_notice.menu_2"
              action="cookie_notice.action_server"/>
    -->
  </data>
</odoo>